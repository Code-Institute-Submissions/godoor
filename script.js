window.addEventListener("load",
function() {
    //Draw box setup
    var draw_width = 800;
    var draw_height = 600;
    var gameLive = true;

    //Levevl setup
    var currentLevel = 1;
    var currentLife = 5;
    var colour = "#" + ((1 << 24) * Math.random() | 0).toString(16);
}



)